export interface RarityStat {
    rarity: number;
    numberOfCharacters: number;
}

export interface LevelStats {
    averageWeaponLevel: number;
    averageCharacterLevel: number;
    averageArtifactLevel: number;
}